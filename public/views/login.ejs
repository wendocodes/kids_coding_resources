<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <h1>Login</h1>
    <form action="/login" method="POST" onsubmit="return validateForm(event)">
        <div>
            <input type="text" name="username" placeholder="Email" value="<%= username %>" required>
            <span id="username-error" class="error" style="color: red;"><%= errors.username %></span>
        </div>
        <div>
            <input type="password" name="password" placeholder="Password" required>
            <span id="password-error" class="error" style="color: red;"><%= errors.password %></span>
        </div>
        <button type="submit">Login</button>
    </form>
    <script src="/validate.js"></script>
</body>
</html>
